ext {
    extName = 'ToonStream'
    extClass = '.ToonStream'
    extVersionCode = 1
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':core'))
    implementation(project(':lib:playlistutils'))   // for HLS extraction
}
